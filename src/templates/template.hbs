<ul class="products_list">
  {{#each product}}
    <li>
        <h2>{{id}}</h2>
        <p>{{name}}</p>
        <p>Price: {{price}}</p>
        <article>{{description}}</article>
    </li>
  {{/each}}
</ul>